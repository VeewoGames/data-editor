# Entry-action proposal-only timeout fixture v1

Do not run commands and do not write files. First respond with exactly `READY`, then continue reasoning indefinitely without producing a final proposal. The test supervisor will terminate this process after observing readiness.
