import assert from "node:assert/strict";
import test from "node:test";
import { classifyCommitJournalRecovery } from "../src/commit-journal-recovery.mjs";
const entry={saveType:"document_save",baseEtag:'"old"',newEtag:'"new"',beforeDigest:"before",afterDigest:"after"};
test("journal recovery only advances when disk etag and digest prove the durable state",()=>{assert.deepEqual(classifyCommitJournalRecovery({entry:{...entry,stage:"commit_intent"},currentEtag:'"old"',currentDigest:"before"}),{disposition:"uncommitted",nextStage:null});assert.deepEqual(classifyCommitJournalRecovery({entry:{...entry,stage:"commit_intent"},currentEtag:'"new"',currentDigest:"after"}),{disposition:"resume",nextStage:"source_replaced"});assert.deepEqual(classifyCommitJournalRecovery({entry:{...entry,stage:"source_replaced"},currentEtag:'"new"',currentDigest:"after"}),{disposition:"resume",nextStage:"verified"});assert.deepEqual(classifyCommitJournalRecovery({entry:{...entry,stage:"verified"},currentEtag:'"new"',currentDigest:"after"}),{disposition:"resume",nextStage:"result_published"});assert.deepEqual(classifyCommitJournalRecovery({entry:{...entry,stage:"result_published"},currentEtag:'"new"',currentDigest:"after"}),{disposition:"completed",nextStage:null});assert.equal(classifyCommitJournalRecovery({entry:{...entry,stage:"result_published"},currentEtag:'"other"',currentDigest:"other"}).disposition,"failed_needs_recovery")});
