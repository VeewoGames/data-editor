# 共享视图团队保存入口与个人草稿持久化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 shared view 的团队保存按钮迁到搜索框右侧的稳定锚点位，只在存在可发布草稿时出现且不推动搜索框布局，同时修复 `Lans` 命名 profile 下 shared draft 各更新路径保存时序不一致的问题，确保团队发布仍显式、个人草稿不丢。

**Architecture:** 本次改动分成三层。交互层在 `Toolbar` 的搜索框右侧增加固定宽度的动作锚点容器，团队保存按钮只在有可发布 shared draft 时渲染到该容器中，从而做到“出现/消失但不挤压搜索框”；状态层把 `updateActiveViewDraft()` 与 `updateSharedViewDraftState()` 收敛到统一的 shared draft 持久化 helper，并立即进入 `commitProfileSave()` 串行队列；验证层先锁定源码契约，再用 Playwright 分别覆盖内容 draft 与结构/顺序 draft 两类回归。

**Tech Stack:** React + TypeScript、Node.js、node:test、Playwright、现有 `saveViewProfile` / `commitProfileSave` / `flushPendingProfileSave` / shared view 状态层

---

## 文件结构

- 修改 `src/components/Toolbar.tsx`
  - 增加搜索框右侧的 `toolbar-search-actions` 固定锚点容器
  - 新增团队保存按钮 props / snapshot 字段
  - 保证按钮只在有可发布草稿时出现，但锚点容器始终占位
- 修改 `src/components/ViewFilterBar.tsx`
  - 删除 `onSaveForEveryone`
  - 删除 `.save-shared`
  - 保留 `重置`
- 修改 `src/App.tsx`
  - 计算团队保存按钮是否显示/是否可用/tooltip
  - 新增 `updateSharedViewDraftAndPersist(...)`
  - 新增 `replaceSharedViewDraftStateAndPersist(...)`
  - 统一处理 `commitProfileSave()` 的异步错误
  - 把 `handleSaveViewForEveryone()` 接到 `Toolbar`
- 修改 `src/styles.css`
  - 新增 `toolbar-search-actions` 固定宽度布局
  - 新增团队保存按钮样式和显隐状态
  - 确保按钮出现/消失不导致搜索框宽度变化
- 修改 `tests/view-state.test.mjs`
  - 更新 Toolbar / ViewFilterBar / App 源码契约
  - 补充 shared draft helper、按钮锚点容器和错误处理静态断言
- 修改 `tests/data-editor.spec.ts`
  - 覆盖 Toolbar 团队保存按钮只在需要保存时出现，且搜索框布局不变化
  - 覆盖命名 profile 下 filter/sort 内容 draft 回归
  - 覆盖 view switch / view order / structure draft 回归

## 实施阶段

### 阶段 1：先锁定源码契约

- 目标：先让测试定义 Toolbar / ViewFilterBar / App 的新边界
- 范围：`tests/view-state.test.mjs`

### 阶段 2：迁移团队保存按钮并固定搜索区布局

- 目标：把团队保存入口从 `ViewFilterBar` 挪到搜索框右侧锚点容器
- 范围：`Toolbar.tsx`、`ViewFilterBar.tsx`、`App.tsx`、`styles.css`

### 阶段 3：统一 shared draft 持久化和错误处理

- 目标：让内容草稿与结构草稿都立即进入 `commitProfileSave()` 队列，并在失败时给出可见反馈
- 范围：`App.tsx`

### 阶段 4：端到端回归

- 目标：证明 `Lans` profile 下内容 draft 和结构/顺序 draft 都不再因未发布团队配置而丢失
- 范围：`tests/data-editor.spec.ts`

## 当前执行假设

- 团队发布语义保持不变，仍通过 `handleSaveViewForEveryone()` 更新 `shared-views.json`
- 当前 Toolbar 允许在搜索框右侧插入固定宽度动作容器
- 当前 `commitProfileSave()` 的串行 promise 队列可复用，不新增新的持久化通道
- shared draft 保存失败至少需要进入已有 `status` 反馈通道，不能继续静默失败
- 本轮不做 profile / localStorage 双写兜底
- 团队保存按钮采用规则 A：只有存在可发布 draft 时才出现；无 draft 时按钮不渲染，但搜索框右侧锚点容器保留

## Task 1: 锁定 Toolbar / ViewFilterBar / App 的新契约

**Files:**
- Modify: `tests/view-state.test.mjs`
- Reference: `src/components/Toolbar.tsx`
- Reference: `src/components/ViewFilterBar.tsx`
- Reference: `src/App.tsx`

- [ ] **Step 1: 写 Toolbar / ViewFilterBar 接线的失败测试**

```js
test("Toolbar owns shared view publish action and ViewFilterBar only keeps reset", async () => {
  const toolbarSource = await readFile(new URL("../src/components/Toolbar.tsx", import.meta.url), "utf8");
  const filterBarSource = await readFile(new URL("../src/components/ViewFilterBar.tsx", import.meta.url), "utf8");
  const appSource = await readFile(new URL("../src/App.tsx", import.meta.url), "utf8");

  assert.match(toolbarSource, /sharedViewPublishVisible: boolean/);
  assert.match(toolbarSource, /sharedViewPublishEnabled: boolean/);
  assert.match(toolbarSource, /sharedViewPublishTooltip: string/);
  assert.match(toolbarSource, /toolbar-search-actions/);
  assert.match(toolbarSource, /保存团队共享视图/);
  assert.doesNotMatch(filterBarSource, /onSaveForEveryone/);
  assert.doesNotMatch(filterBarSource, /为所有人保存/);
  assert.match(appSource, /onSaveSharedViewPublish=\{\(\) => void handleSaveViewForEveryone\(\)\}/);
});
```

- [ ] **Step 2: 写 shared draft helper 与错误处理的失败测试**

```js
test("shared draft persistence uses dedicated helpers and visible error handling", async () => {
  const appSource = await readFile(new URL("../src/App.tsx", import.meta.url), "utf8");

  assert.match(appSource, /function updateSharedViewDraftAndPersist\(/);
  assert.match(appSource, /function replaceSharedViewDraftStateAndPersist\(/);
  assert.match(appSource, /void commitProfileSave\(selectedViewProfileNameRef\.current!, selectedViewProfileRef\.current\)\.catch/);
});
```

- [ ] **Step 3: 写固定锚点容器的失败测试**

```js
test("Toolbar reserves a stable search-side action anchor for shared publish", async () => {
  const toolbarSource = await readFile(new URL("../src/components/Toolbar.tsx", import.meta.url), "utf8");
  const stylesSource = await readFile(new URL("../src/styles.css", import.meta.url), "utf8");

  assert.match(toolbarSource, /<div className="toolbar-search-actions">/);
  assert.match(stylesSource, /\.toolbar-search-actions\s*\{[\s\S]*min-width:/);
  assert.match(stylesSource, /\.toolbar-search-actions\s*\{[\s\S]*display:\s*flex;/);
  assert.match(stylesSource, /\.toolbar-search-actions\s*\{[\s\S]*width:/);
});
```

- [ ] **Step 4: 运行测试确认失败**

Run: `node --test tests/view-state.test.mjs`

Expected: FAIL，原因应是 Toolbar 还没有 `toolbar-search-actions` / shared publish 字段，`ViewFilterBar` 仍有 `onSaveForEveryone`，`App.tsx` 也没有新的 helper

- [ ] **Step 5: Commit**

```bash
git add tests/view-state.test.mjs
git commit -m "test: lock shared publish toolbar and draft persistence contract"
```

## Task 2: 把团队保存按钮迁到搜索框右侧稳定锚点

**Files:**
- Modify: `src/components/Toolbar.tsx`
- Modify: `src/components/ViewFilterBar.tsx`
- Modify: `src/App.tsx`
- Modify: `src/styles.css`
- Test: `tests/view-state.test.mjs`

- [ ] **Step 1: 在 ToolbarSnapshot 和 ToolbarProps 中增加团队保存字段与回调**

```ts
type ToolbarProps = {
  snapshot: ToolbarSnapshot;
  onQueryChange: (value: string) => void;
  onSaveSharedViewPublish: () => void;
  onRefreshBuild: () => void;
  onRestartServer: () => void;
  onCloseServer: () => void;
  onResetView: () => void;
  onSelectViewProfile: (name: string) => void;
  onCreateViewProfile: () => void;
  onChangeTheme: (theme: UiTheme) => void;
  onChangeBaseFontSize: (size: UiPreferences["baseFontSize"]) => void;
  onUnhideField: (fieldName: string) => void;
  onUnhideAllFields: () => void;
};

export type ToolbarSnapshot = {
  currentPath: string | null;
  collectionPath: string;
  viewProfiles: string[];
  selectedViewProfileName: string | null;
  activeThemeId: UiTheme;
  baseFontSize: UiPreferences["baseFontSize"];
  rowCount: number;
  visibleCount: number;
  query: string;
  autosaveState: AutosaveState;
  commandSaving: boolean;
  closing: boolean;
  rebuilding: boolean;
  restarting: boolean;
  status: string;
  hiddenFields: string[];
  sharedViewPublishVisible: boolean;
  sharedViewPublishEnabled: boolean;
  sharedViewPublishTooltip: string;
};
```

- [ ] **Step 2: 在搜索框右侧加入固定锚点容器**

```tsx
<ExpandableSearch className="search-box" value={snapshot.query} alwaysExpanded onChange={props.onQueryChange} placeholder="搜索当前表格" />
<div className="toolbar-search-actions">
  {snapshot.sharedViewPublishVisible ? (
    <button
      aria-label="保存团队共享视图"
      className={snapshot.sharedViewPublishEnabled
        ? "ghost-button icon-button toolbar-action-button toolbar-shared-publish-button is-dirty"
        : "ghost-button icon-button toolbar-action-button toolbar-shared-publish-button"}
      disabled={!snapshot.sharedViewPublishEnabled}
      onClick={props.onSaveSharedViewPublish}
      title={snapshot.sharedViewPublishTooltip}
      type="button"
    >
      <icons.save size={16} />
    </button>
  ) : null}
</div>
<div className="toolbar-spacer" />
```

- [ ] **Step 3: 从 ViewFilterBar 删除团队保存入口**

```tsx
export type ViewFilterBarProps = {
  snapshot: ViewFilterBarSnapshot;
  onChangeFilters: (filters: FilterGroup) => void;
  onChangeSorts: (sorts: SortRule[]) => void;
  onAddFilter: (field: string, fieldType: FieldDisplayType) => void;
  onAutoOpenRuleHandled: () => void;
  onResetView: () => void;
  onCreateFormalOption?: (input: CreateFilterOptionInput) => Promise<MultiSelectOptionView[]>;
};

{showSharedViewActions ? (
  <div className="view-filter-actions">
    <button type="button" className="view-tab-action" onClick={onResetView}>
      重置
    </button>
  </div>
) : null}
```

- [ ] **Step 4: 在 App.tsx 中显式计算 Toolbar 需要的字段**

```ts
const currentSharedDraftState = currentSharedViewDraftState();
const sharedViewPublishVisible = Boolean(
  activeCollectionKey
  && activeSharedView
  && hasViewDraft(currentSharedDraftState, activeCollectionKey, activeSharedView.id),
);
const sharedViewPublishEnabled = Boolean(
  activeCollectionKey
  && activeSharedView
  && !commandSaving
  && hasViewDraft(currentSharedDraftState, activeCollectionKey, activeSharedView.id),
);
const sharedViewPublishTooltip = sharedViewPublishEnabled
  ? "保存团队共享视图"
  : "当前没有可发布的团队视图改动";

const toolbarSnapshot = useMemo<ToolbarSnapshot>(() => ({
  currentPath: selectedPath,
  collectionPath,
  viewProfiles,
  selectedViewProfileName,
  activeThemeId: uiPreferences.activeThemeId,
  baseFontSize: uiPreferences.baseFontSize,
  rowCount: rows.length,
  visibleCount: visibleRowViews.length,
  query: activeViewRenderState.query,
  autosaveState,
  commandSaving,
  closing,
  rebuilding,
  restarting,
  status: statusText,
  hiddenFields,
  sharedViewPublishVisible,
  sharedViewPublishEnabled,
  sharedViewPublishTooltip,
}), [
  selectedPath,
  collectionPath,
  viewProfiles,
  selectedViewProfileName,
  uiPreferences.activeThemeId,
  uiPreferences.baseFontSize,
  rows.length,
  visibleRowViews.length,
  activeViewRenderState.query,
  autosaveState,
  commandSaving,
  closing,
  rebuilding,
  restarting,
  statusText,
  hiddenFields,
  sharedViewPublishVisible,
  sharedViewPublishEnabled,
  sharedViewPublishTooltip,
]);
```

- [ ] **Step 5: 调整 CSS，保证按钮出现/消失不推挤搜索框**

```css
.toolbar-search-actions {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  min-width: 32px;
  width: 32px;
  margin-left: 8px;
}

.toolbar-shared-publish-button {
  width: 32px;
  height: 32px;
}

.toolbar-shared-publish-button.is-dirty {
  color: #d9730d;
  border-color: rgba(217, 115, 13, 0.28);
  background: rgba(217, 115, 13, 0.1);
}

.toolbar-shared-publish-button:disabled {
  opacity: 0.52;
}
```

- [ ] **Step 6: 运行契约测试确认通过**

Run: `node --test tests/view-state.test.mjs`

Expected: PASS；Toolbar 新锚点与 `ViewFilterBar` 删除团队保存的断言通过

- [ ] **Step 7: Commit**

```bash
git add src/components/Toolbar.tsx src/components/ViewFilterBar.tsx src/App.tsx src/styles.css tests/view-state.test.mjs
git commit -m "feat: move shared publish button next to toolbar search"
```

## Task 3: 统一 shared draft 持久化并补错误反馈

**Files:**
- Modify: `src/App.tsx`
- Test: `tests/view-state.test.mjs`

- [ ] **Step 1: 新增 shared draft 持久化 helper**

```ts
function reportSharedDraftSaveFailure(error: unknown) {
  setStatus(error instanceof Error ? error.message : String(error));
}

function updateSharedDraftProfileAndPersist(next: SharedViewDraftState) {
  mutateSelectedViewProfile((draft) => {
    draft.lastActiveViews = next.lastActiveViews;
    draft.viewDrafts = next.viewDrafts;
    draft.viewOrderDrafts = next.viewOrderDrafts;
    draft.structureDrafts = next.structureDrafts;
  });
  selectedViewProfileRef.current = {
    ...selectedViewProfileRef.current,
    lastActiveViews: next.lastActiveViews,
    viewDrafts: next.viewDrafts,
    viewOrderDrafts: next.viewOrderDrafts,
    structureDrafts: next.structureDrafts,
  };
  setViewDraftDirty(hasSharedDrafts(next));
  void commitProfileSave(selectedViewProfileNameRef.current!, selectedViewProfileRef.current).catch(reportSharedDraftSaveFailure);
}

function updateSharedViewDraftAndPersist(mutator: (draft: SharedViewDraftState) => SharedViewDraftState) {
  if (!selectedViewProfileName) return false;
  updateSharedDraftProfileAndPersist(mutator(currentSharedViewDraftState()));
  return true;
}

function replaceSharedViewDraftStateAndPersist(next: SharedViewDraftState) {
  if (!selectedViewProfileName) return false;
  updateSharedDraftProfileAndPersist(next);
  return true;
}
```

- [ ] **Step 2: 让内容 draft 更新走 helper**

```ts
function updateActiveViewDraft(patch: Partial<CollectionView>) {
  if (commandSaving || !activeCollectionKey || !activeSharedView) return;
  const viewId = activeSharedView.id;
  if (updateSharedViewDraftAndPersist((current) => ({
    lastActiveViews: { ...current.lastActiveViews },
    viewDrafts: {
      ...current.viewDrafts,
      [activeCollectionKey]: {
        ...(current.viewDrafts[activeCollectionKey] ?? {}),
        [viewId]: {
          ...(current.viewDrafts[activeCollectionKey]?.[viewId] ?? {}),
          ...patch,
        },
      },
    },
    viewOrderDrafts: { ...current.viewOrderDrafts },
    structureDrafts: { ...current.structureDrafts },
  }))) {
    return;
  }

  setLocalSharedViewDrafts((current) => {
    const next = {
      lastActiveViews: { ...current.lastActiveViews },
      viewDrafts: {
        ...current.viewDrafts,
        [activeCollectionKey]: {
          ...(current.viewDrafts[activeCollectionKey] ?? {}),
          [viewId]: {
            ...(current.viewDrafts[activeCollectionKey]?.[viewId] ?? {}),
            ...patch,
          },
        },
      },
      viewOrderDrafts: { ...current.viewOrderDrafts },
      structureDrafts: { ...current.structureDrafts },
    };
    writeLocalSharedViewDrafts(window.localStorage, next);
    return next;
  });
  setViewDraftDirty(true);
}
```

- [ ] **Step 3: 让结构/顺序/lastActiveViews 更新走 helper**

```ts
function updateSharedViewDraftState(next: SharedViewDraftState) {
  if (replaceSharedViewDraftStateAndPersist(next)) return;
  setLocalSharedViewDrafts(next);
  writeLocalSharedViewDrafts(window.localStorage, next);
  setViewDraftDirty(hasSharedDrafts(next));
}
```

- [ ] **Step 4: 写并运行源码契约测试，确认 helper 与错误处理通过**

Run: `node --test tests/view-state.test.mjs`

Expected: PASS；`updateSharedViewDraftAndPersist`、`replaceSharedViewDraftStateAndPersist`、`.catch(reportSharedDraftSaveFailure)` 断言通过

- [ ] **Step 5: Commit**

```bash
git add src/App.tsx tests/view-state.test.mjs
git commit -m "fix: persist named-profile shared drafts through unified helpers"
```

## Task 4: 用 Playwright 覆盖按钮显隐与内容 draft 回归

**Files:**
- Modify: `tests/data-editor.spec.ts`
- Reference: `src/components/Toolbar.tsx`

- [ ] **Step 1: 写 Toolbar 按钮显隐的失败测试**

```ts
test("toolbar shared publish button uses a stable search-side anchor and only appears when draft exists", async ({ page }) => {
  await page.goto("/");
  await page.locator('.sidebar-item[title="data/e2e_select.json"]').click();

  const anchor = page.locator(".toolbar-search-actions");
  const publish = page.locator(".toolbar-shared-publish-button");
  const widthBefore = await anchor.evaluate((node) => node.getBoundingClientRect().width);

  await expect(anchor).toBeVisible();
  await expect(publish).toHaveCount(0);

  await page.getByRole("button", { name: "+ 筛选" }).click();
  await page.locator(".add-filter-field-option").filter({ hasText: "category" }).click();
  await page.locator(".filter-option-row").filter({ hasText: "spell" }).click();

  await expect(publish).toHaveCount(1);
  const widthAfter = await anchor.evaluate((node) => node.getBoundingClientRect().width);
  expect(widthAfter).toBe(widthBefore);
});
```

- [ ] **Step 2: 写命名 profile 内容 draft 持久化的失败测试**

```ts
test("named profile content drafts survive reload and file switches without team publish", async ({ page }) => {
  await page.goto("/");
  await page.evaluate(async () => {
    localStorage.clear();
    await fetch("/api/view-profile", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        name: "shared_draft_content_profile",
        profile: { sidebarWidth: null, collections: {} },
      }),
    });
    localStorage.setItem("data-editor:selected-view-profile", "shared_draft_content_profile");
  });

  await page.reload();
  await page.locator('.sidebar-item[title="data/e2e_select.json"]').click();
  await page.getByRole("button", { name: "+ 筛选" }).click();
  await page.locator(".add-filter-field-option").filter({ hasText: "category" }).click();
  await page.locator(".filter-option-row").filter({ hasText: "spell" }).click();

  await page.locator('.sidebar-item[title="data/e2e_select_long.json"]').click();
  await page.locator('.sidebar-item[title="data/e2e_select.json"]').click();
  await page.reload();
  await page.locator('.sidebar-item[title="data/e2e_select.json"]').click();
  await expect(page.locator(".view-filter-chip:not(.sort-chip)").filter({ hasText: "category" })).toHaveCount(1);
});
```

- [ ] **Step 3: 运行定向测试确认先失败**

Run: `npx playwright test tests/data-editor.spec.ts -g "toolbar shared publish button uses a stable search-side anchor and only appears when draft exists|named profile content drafts survive reload and file switches without team publish"`

Expected: FAIL；按钮显隐或内容 draft 回归尚未满足

- [ ] **Step 4: 修正断言后跑到通过**

Run: `npx playwright test tests/data-editor.spec.ts -g "toolbar shared publish button uses a stable search-side anchor and only appears when draft exists|named profile content drafts survive reload and file switches without team publish"`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tests/data-editor.spec.ts
git commit -m "test: cover toolbar shared publish visibility and content draft persistence"
```

## Task 5: 用 Playwright 覆盖结构 / 顺序 draft 回归

**Files:**
- Modify: `tests/data-editor.spec.ts`
- Reference: `src/App.tsx`

- [ ] **Step 1: 写 view switch / structure draft 回归的失败测试**

```ts
test("named profile structural shared drafts survive view switches and reload without team publish", async ({ page }) => {
  await page.goto("/");
  await page.evaluate(async () => {
    localStorage.clear();
    await fetch("/api/view-profile", {
      method: "POST",
      headers: { "content-type": "application/json" },
      body: JSON.stringify({
        name: "shared_draft_structure_profile",
        profile: { sidebarWidth: null, collections: {} },
      }),
    });
    localStorage.setItem("data-editor:selected-view-profile", "shared_draft_structure_profile");
  });

  await page.reload();
  await page.locator('.sidebar-item[title="data/runes.json"]').click();
  await expect(page.locator(".view-tab-shell")).toHaveCountGreaterThan(1);

  const firstLabel = await page.locator(".view-tab-shell").nth(0).innerText();
  const secondLabel = await page.locator(".view-tab-shell").nth(1).innerText();

  await page.evaluate(() => {
    const source = document.querySelectorAll(".view-tab-shell")[1];
    const target = document.querySelectorAll(".view-tab-shell")[0];
    if (!(source instanceof HTMLElement) || !(target instanceof HTMLElement)) return;
    const sourceRect = source.getBoundingClientRect();
    const targetRect = target.getBoundingClientRect();
    source.dispatchEvent(new PointerEvent("pointerdown", { bubbles: true, clientX: sourceRect.left + 8, clientY: sourceRect.top + 8, pointerId: 1 }));
    window.dispatchEvent(new PointerEvent("pointermove", { bubbles: true, clientX: targetRect.left + 8, clientY: targetRect.top + 8, pointerId: 1 }));
    window.dispatchEvent(new PointerEvent("pointerup", { bubbles: true, clientX: targetRect.left + 8, clientY: targetRect.top + 8, pointerId: 1 }));
  });

  await page.reload();
  await page.locator('.sidebar-item[title="data/runes.json"]').click();
  await expect(page.locator(".view-tab-shell").nth(0)).toContainText(secondLabel);
  await expect(page.locator(".view-tab-shell").nth(1)).toContainText(firstLabel);
});
```

- [ ] **Step 2: 运行定向测试确认先失败**

Run: `npx playwright test tests/data-editor.spec.ts -g "named profile structural shared drafts survive view switches and reload without team publish"`

Expected: FAIL；当前结构/顺序 draft 还不能稳定恢复

- [ ] **Step 3: 调整交互细节并跑到通过**

Run: `npx playwright test tests/data-editor.spec.ts -g "named profile structural shared drafts survive view switches and reload without team publish"`

Expected: PASS

- [ ] **Step 4: 跑最小必要验证集**

Run: `node --test tests/view-state.test.mjs`

Expected: PASS

Run: `npx playwright test tests/data-editor.spec.ts -g "toolbar shared publish button uses a stable search-side anchor and only appears when draft exists|named profile content drafts survive reload and file switches without team publish|named profile structural shared drafts survive view switches and reload without team publish"`

Expected: PASS

Run: `npm run typecheck`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add tests/data-editor.spec.ts tests/view-state.test.mjs
git commit -m "test: cover structural shared draft persistence in named profiles"
```

## Spec Coverage Check

- 团队保存按钮迁到搜索框右侧：Task 2
- 只有需要保存时才出现：Task 2 + Task 4
- 出现/消失不导致搜索框布局变化：Task 2 + Task 4
- `ViewFilterBar` 去掉团队保存只保留 `重置`：Task 2
- `updateActiveViewDraft()` 与 `updateSharedViewDraftState()` 都立即入队：Task 3
- `Lans` profile 下内容 draft 不丢：Task 4
- `Lans` profile 下结构/顺序 draft 不丢：Task 5
- 保持团队发布显式语义：Task 2 + Task 4

## 自检结果

- 已移除占位内容，没有 `TODO` / `TBD` / 注释式留白
- 计划明确区分内容 draft 与结构/顺序 draft 两条验证链
- 团队保存按钮的“只在需要保存时出现，但不推动搜索框布局”已经落到具体容器和样式约束
